[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA routing fix (React Router / client-side routing)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
